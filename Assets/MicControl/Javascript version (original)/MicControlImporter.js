#pragma strict
/*
import System;
import System.IO;
import UnityEngine;
import UnityEditor;

@CustomEditor(MicControl)

class MicControlImporter extends Editor  {

//apply only to this audio source
		 var CurrentMicController = Selection.activeGameObject;

	function OnInspectorGUI() {
	
	//Redirect 3D toggle
CurrentMicController.GetComponent(MicControl).ThreeD=GUILayout.Toggle(CurrentMicController.GetComponent(MicControl).ThreeD,GUIContent ("3D sound","Should the streamed audio be a 3D sound? (Only enable this if you are using the controller to stream sound (VOIP) "));
	
	}
	
}

*/